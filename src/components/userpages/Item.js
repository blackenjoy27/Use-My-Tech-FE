import React from "react";
import { TheItem } from "../styledcomponents/StyledEle";

const Item = (props) => {
    const { name, imgLink, price } = props;

    return (
        <TheItem>

        </TheItem>
    )
}